# 优学院刷课页面真实数据结构调研

> 调研时间：2026-08-27
> 环境：Edge 151 + `--remote-debugging-port=9222`，标签页 `ua.dgut.edu.cn/learnCourse/learnCourse.html`
> 采样课程：《英语话中华》（courseId=60216，章节已过期）、《管理统计学》（courseId=18046）
> 方法：全部通过 CDP `Runtime.evaluate` 只读采样，未在未答测验上产生任何点击。

## 0. 一句话结论

测验题目、选项、提交按钮**全部渲染在 learnCourse 主文档里（无 iframe、无图片、无 OCR 必要）**，
页面是 Knockout.js 驱动的 SPA，DOM 结构高度规整，用 CSS 选择器 + `getBoundingClientRect` 坐标 +
CDP `Input.dispatchMouseEvent` 即可实现完整的自动作答链路。

## 1. 连接层

| 项 | 值 |
|---|---|
| 调试端口 | 9222（`yxy_backend.py:35` 默认值，与 `browser_profile` 独立 profile 配套） |
| 标签页定位 | `/json/list` 里 `type=page` 且 URL 含 `ua.dgut.edu.cn/learnCourse` |
| websocket | 直接连标签页自身的 `webSocketDebuggerUrl` 即可，**测验不需要附加 iframe 子会话** |
| 登录态 | 持久保存在 `browser_profile/`，重启浏览器免登录 |

主文档里存在 `div.catalog-col-iframe-quiz#knowledgeQuiz` 容器（宽度恒 0），测验并不在其中渲染，
只是个占位；`iframe[src*="ulearning.cn"]` 是课件（视频/文档）用的，与测验无关。

## 2. 左侧目录（自动导航用）

层级：`.catalog-list > .chapter-item`（章）`> .section-item`（节）`> .page-item > .page-name`（页面节点）。

```
div.chapter-item
├── div.chapter-name.cursor > .text        # 章名，点击折叠/展开
└── div.section-list
    └── div.section-item
        ├── div.section-name.cursor > .text  # 节名
        └── div.page-list
            └── div.page-item
                └── div.page-name.cursor      # 叶子节点 = 一个学习页面
                    └── .text > .page-icon + <span>节点名</span>
```

状态类（挂在 `.page-name` 上，由 Knockout `css` 绑定驱动）：

| 类 | 含义 |
|---|---|
| `active` | 当前打开的节点（`$root.currentPage() == page`） |
| `complete` | `page.record().status()` 为真 |

**实测注意**：已答完的测验节点 `complete` 仍是 false。原因是章节过期横幅
"当前章节已过期，可以继续浏览，但不保存记录"——过期章节不写学习记录。
不能依赖目录勾选状态判断测验是否已答，必须进页面读 `.question-wrapper.finished`。

- 每章末尾有一个"课堂测验"节点（《管理统计学》6 章共 7 个测验节点，名称全部相同）。
- `contentType`（从根 VM 目录树全量读取）：**5=重点提要类富文本页，6=音视频/文档页，7=交互页**。
  注意 7 **不是**测验专属——普通视频页也是 7（ulearning 的页面由元素组成：视频元素+测验元素，
  视频中途插题即"7 号交互页内同时含视频元素和 question 元素"）；"课堂测验"节点是只含测验元素的 7 号页。
- 学习记录状态两种取值：`record().status` 为 `0`（有记录、未完成）或 `undefined`（从未打开过、无记录）。
  答完测验后 status 仍可能停在 0（过期章节不保存记录所致），不能作为已答判据。
- 根视图模型上有 `prevPageFamily` / `nextPageFamily` / `prevPageName` / `nextPageName`，可用于"下一个节点"导航。

## 3. 测验页 DOM 结构（核心）

```html
<div class="question-view">
  <div class="question-setting-panel">          # "练习 共10题 限答1次 40分"
    <div class="question-setting-list">
      <div class="question-setting-item">共10题</div>   # 每项一个
      <div class="question-setting-item">限答1次</div>
      <div class="question-setting-item">40分</div>
    </div>
  </div>
  <div class="question-element-node-list">       # 一页卷：所有题纵向排列
    <div class="question-element-node">
      <div class="question-wrapper" id="question19545176">
        <div class="split-screen-wrapper">
          <div class="question-title-wrapper">
            <div class="question-title-scroller">
              <div class="question-title-text not-IE">
                <span class="question-sort">1</span><span class="question-sort-dot">.</span>
                <span class="question-type-tag">单选题</span>
                <span class="question-title-html question-type-1">题干文本</span>
          <div class="question-body-wrapper">
            <!-- type 1/2: -->
            <div class="choice-type"><div class="choice-list clearfix">
              <a class="choice-item clearfix" id="choice46869210" role="button">
                <div class="checkbox radio">      # 选中态挂这里：.selected；对错态 .right/.wrong
                  <i class="iconfont right"></i><i class="iconfont wrong"></i>
                <div class="option">A.</div>       # 选项字母
                <div class="content-wrapper"><div class="content"><div class="text">选项内容</div></div></div>
            <!-- type 4 (判断题): -->
            <div class="checking-type">
              <div class="choice-btn right-btn" role="button">   # "正确"
              <div class="choice-btn wrong-btn" role="button">   # "错误"
        <div class="question-operation-wrapper">
          <button class="btn-submit">提交</button>   # 绑 click: submitQuestion（见 §5：实际隐藏）
      <div class="question-analysis-wrapper">       # 答后显示
        <div class="answer-result-tip">回答正确/回答错误</div>
        <div class="answer-result-text">
          <div class="correct-answer-area">正确答案： D</div>
          <div class="user-answer-area">你的答案： C</div>
          （答案解析：...）
  <div class="question-operation-area">
    <button class="btn-submit">提交</button>         # 绑 click: submitQuiz —— 真正用的提交按钮
  <div class="video-question-end">                  # 视频插题模式才有
    <div class="video-question-end-tip">答题结束，3s后视频继续播放</div>
```

元素 id 规律：`question{id}`、`choice{id}`，id 是服务端数据主键，可作为稳定引用键。

## 4. 题型对照表（`question.type` / `.question-type-tag` / 模板分支）

| type | 题型 | 标签文本 | 作答控件 | 已证实来源 |
|---|---|---|---|---|
| 1 | 单选题 | `单选题` | `a.choice-item`，`.checkbox.radio` 单选样式 | 英语话中华 + 管理统计学（已采样） |
| 2 | 多选题 | （推断） | `a.choice-item`，`.checkbox` 无 radio | 模板 `type()==1 \|\| type()==2` 共用分支；未实际采样 |
| 3 | 填空题 | `填空题` | 题干内嵌 `span.answer-width`（一空一个，160×40），正确答案用 `arrayToString` 拼接 | 管理统计学（已采样，已答态） |
| 4 | 判断题 | `判断题` | `.checking-type` 下 `.choice-btn.right-btn` / `.wrong-btn` 两个 div | 管理统计学（已采样 outerHTML） |
| 24 | 材料/复合题 | （未采样） | `question-wrapper.sub-question`、`parent-question` 类，分屏 `.split-screen-mode` | 模板分支 |

标题类名规律：`question-title-html question-type-{type}`，填空题额外带 `multiple`。
模板中还有 5/11/12/17/23 等类型的条件分支（阅读理解/口语类，公选课罕见），未采样。

## 5. 提交机制（重要）

页面上有**两个**提交按钮，绑定不同函数：

| 位置 | 绑定 | 实测可见性 |
|---|---|---|
| `.question-operation-wrapper`（题内） | `submitQuestion`（单题提交） | **恒隐藏**（rect 全 0），模板留壳 |
| `.question-operation-area`（卷尾，`question-view` 直接子级） | `submitQuiz`（整套提交） | 未完成时可见，约 120×40，右下角 |

因此作答流程是：**点选项 → 点卷尾 `.question-operation-area .btn-submit` → 该题变 `finished`**。
每答完一题解析区立即出现（`show-answer` 状态下），单题一交，不存在"全部答完再交卷"的独立步骤。

未答态实测补充：《管理统计学》未答单选题页上，卷尾提交按钮可见 `rect=[854,507,120,40]`；
题内 `.question-operation-wrapper` 的 computed `display: none`，从渲染层面证实恒隐藏。
`question-view` 在未答时无任何附加状态类（已答后才有 `hide-analysis` 等切换）。
限答次数字段 `questionManyChance = 0`（0 = 无额外作答机会，即"限答1次"）。

### 5.1 弹窗机制（两种，实测）

**弹窗 A：未答完确认框**（页内切换时触发，"下一页"指向未答完测验的页面）

```html
<div class="modal-backdrop fade in">              <!-- z-index 1040 -->
<div class="modal fade in">                        <!-- z-index 1050，全屏容器 -->
  文本："本页面还有题目没有完成，确定要切换页面吗？"
  <div class="modal-operation">
    <button class="btn-submit">  留在本页 </button>   <!-- 取消 -->
    <button class="btn-hollow">  确定离开 </button>   <!-- 确认放弃，rect=[786,506,100,40] -->
  </div>
</div>
```

**弹窗 B：章节统计页**（跨章时触发，即"章节末尾还会再弹一次"的那一个；另有小节版 `sectionStatPage`）

```html
<div class="modal fade in">
  <div class="modal-dialog"><div class="modal-content"><div class="modal-body">
    <div class="stat-page chapter-stat">            # progress==100 时追加 .complete
      <div class="stat-data-area">
        本章成绩（totalScore，如 "0分"）/ 完成度（chapterProgress，如 "0%"）/ 累计学习时间（totalStudyTime）
      <div class="stat-message text-default">
        .complete 分支："恭喜你完成本章的学习……"
        .incomplete 分支："本章有部分页面未完成学习……"
      <div class="stat-next">
        <button class="btn-hollow"> << 留在本页 </button>      <!-- rect=[524,430,130,38] -->
        <button class="btn-hollow"> 继续下一章 >> </button>     <!-- rect=[874,430,130,38] -->
```

**弹窗 C：走神检测 / 计时暂停**（长时间无交互时触发，实测出现于刷课中途）

```html
<div class="modal fade in">                        <!-- 同一个 .modal 壳 -->
  <div class="modal-content">
    <i class="iconfont close-btn" data-dismiss="modal">  <!-- 右上角关闭 -->
    <div class="modal-image">  <!-- 图片随 modalType 切换，suspend → suspend.png -->
    <div class="modal-body">
      <div class="modal-info">   <!-- modalType == 'suspend' 分支 -->
        <div class="title">计时学习已暂停</div>
        <div class="content">走神太久啦！计时君停止工作~(中断学习<span id="noStudyTime">00:01:26</span>)</div>
      </div>
      <div class="modal-operation">
        <button class="btn-submit">继续学习</button>   <!-- 唯一按钮 -->
      </div>
```

**同一个 `.modal` 壳承载全部平台弹窗**，由根 VM 的 `$root.modalType()` 切换内容分支，
完整目录（从 data-bind 图片映射读出）：`videoGuide / incomplete / splitScreen / multiLearning /
stopLearning / createRecordFailed / goBackCreateRecordFailed / createRecordFailedTooMany /
suspend / timeLimit / autoSubmit / docFailed / flashFailed / videoFailed / incompleteTimeLimit /
incompleteOralItem / videoNoWifi / docNoWifi`。

要点：
- **走神检测对刷机影响最大**：挂机看视频一段时间后必弹 suspend，且**学习计时暂停**——不点掉
  的话进度/时长不再累计。单按钮 `.btn-submit`"继续学习"即可恢复。
- 弹窗处理器的按钮匹配必须包含"继续学习"这类文案，不能只认"确定离开/继续下一章"。
- 通用兜底策略：`.modal` 可见且 `.modal-operation` 里只有一个按钮时，点它必然是"继续/确认"语义。

要点（弹窗 A/B 沿用）：
- 判存：`.modal.fade.in` 可见（`getBoundingClientRect().width > 0`）即有弹窗；**按钮必须按文本模糊匹配**（三种弹窗按钮文本不同：`留在本页`/`确定离开`、`<< 留在本页`/`继续下一章 >>`、`继续学习`），不能只按 class。
- 章节末尾连弹 A→B 两重，脚本处理必须循环（检测→按策略点→再检测，上限 3 次以上）；"继续下一章/确定离开"是前进方向，"留在本页"是取消。
- 弹窗 B 的 `.stat-data` 是现成的章学习进度遥测（成绩/百分比/时长），可直接读出来写日志。
- 弹窗 A 说明"部分作答"是允许的；当前自动化不再跳过填空题，而是逐空输入英文逗号 `,`。
  注意主动离开未答完的测验 = 该测验保持未完成。

## 6. 状态判定规则（修正早期错误假设）

`.question-wrapper` 的类由 Knockout 绑定生成：

```js
css: {
  finished:    question.status(),          // ★ 已作答判定只能用它
  show-answer: question.questionShowAnswer, // 教师端"显示答案"开关，与是否已答无关
  right:       question.isCorrect(),
  wrong:       !question.isCorrect(),
  sub-question: question.parentQuestion
}
```

**未答题的 wrapper 也会带 `show-answer wrong`**（初始 `isCorrect()=false`）——
早期用 `show-answer` 判断已答是错的，正确判据：

- 已答 ⇔ `class` 含 **`finished`**（等价 `question.status()` 非零）
- 选项选中 ⇔ 选项内 `.checkbox.selected`（`choice.isSelected()`，同时 aria-label 变为 " 已选中"）
- 判断题选中 ⇔ `.choice-btn.selected`
- 解析可见 ⇔ `.question-analysis-wrapper.show`

"限答1次"由 `questionLimitChance()` / `pageElement.usedSubmitChance()` 控制；
`question-view` 根节点有 `hide-analysis` 类切换逻辑（剩余次数>0 时隐藏解析）。

## 7. 答后解析区（答案来源）

`.question-analysis-wrapper.show` 内：

```
.answer-result-tip  → "回答正确！" / "回答错误！"
.correct-answer-area → "正确答案： D"     # 判断题渲染为 "正确"/"错误"
.user-answer-area   → "你的答案： C"
"答案解析：" + html
```

**答一题立即出正确答案**（在 `questionShowAnswer` 开启时）。这意味着可以先以任意选项作答、
从解析区读出正确答案。但"限答1次"下没有重答机会，此特性只能用于记录，不能改分。

## 8. Knockout 视图模型（绕过 DOM 的数据通道）

`window.ko` 不存在，但 `window.koLearnCourseViewModel`（根 VM）存在，且 ko 本体可从 requirejs 拿：

```js
const ko = require('knockout');            // 或 requirejs('knockout')
const qVm = ko.dataFor(document.querySelector('.question-wrapper'));
// qVm.question.choices() → [{id:17536380, title:"正态分布", option:"A", linkType:0, link:""}]
// qVm.clickChoice / clickCheckingBtn / submitQuestion 等 action 也在 VM 上
const root = window.koLearnCourseViewModel;
// root.currentPage() → {name:"单个总体比例与方差的区间估计", contentType:7, record().status}
// root.currentChapter/currentSection/prevPageFamily/nextPageFamily
```

实测样例（未答题）：`question.type()` 可读，`status()`/`isCorrect()`/`correctAnswer()` 在未答时为
空值。服务端是否把 `correctAnswer` 提前下发给客户端**未确认**（未答状态下读到的均为 undefined，
不能假设能直接读答案）。

## 9. 视频中途插题（管理统计学实测）

测验组件内嵌 `.video-question-end`（"答题结束，3s后视频继续播放"）表明：视频播放到预设时间点会
暂停并弹出测验（同一套 `.question-view` 组件），提交后 3 秒视频自动续播。与 `yxy_course.py`
刷课循环的联动要求：视频控制循环检测到 `.question-view` 出现时挂起速度控制/翻页逻辑，等待
所有题 `finished` 且 `video-question-end` 出现后再恢复。

## 10. 点击/坐标约定

- `getBoundingClientRect()` 返回视口坐标，与 `Input.dispatchMouseEvent` 的坐标系一致，无需换算。
- 选项行高实测 56px、宽 824px（1528 宽视口），卷尾提交按钮 120×40。
- 长页一页卷模式下，点击前必须 `scrollIntoView({block:'center'})` 后**重新取 rect**（滚动改变坐标），
  并校验目标 y 在 `0..innerHeight` 内。
- 真实事件序列：`mouseMoved → mousePressed → mouseReleased`（`button:"left", clickCount:1`），
  产生 `isTrusted=true` 事件，Knockout 的 `click: clickChoice` 绑定正常响应。

## 11. 已采样的真实数据片段

**单选题（未答态，管理统计学）**：

```
question-wrapper（无 finished） id=question7830222
  question-type-tag = 单选题
  question-title-html question-type-1 = 单个总体方差的区间估计中，需要用到下列哪些分布的临界值
  choices:
    A. 正态分布   id=choice17536380  rect=[510,230,824,56]
    B. t分布      id=choice17536381  rect=[510,286,824,56]
    C. 卡方分布   id=choice17536382  rect=[510,342,824,56]
    D. F分布      id=choice17536383  rect=[510,398,824,56]
```

**判断题（已答态，管理统计学）**：`question7738865`，type=4，题干"随机变量是定义在样本空间上的
单值实值函数"，解析区 `正确答案： 错误`；作答区是 `.choice-btn.right-btn` / `.wrong-btn`。

**填空题（已答态，管理统计学）**：`question7522161`，type=3，题干含两个 `span.answer-width`
（"样本容量是＿，样本个数是＿"），一空 160×40。

**英语话中华 10 题练习**：全单选，"共10题 限答1次 40分"，答完后每题
`finished show-answer wrong/right` + 解析区（样本答案分布 D/D/B/C…）。

## 12. 尚未采样/待确认的点

1. **多选题（type 2）**未遇到实例；控件推断与单选相同但 `.checkbox` 无 `radio` 类，选中逻辑是否需要"提交前可改选"未知。
2. ~~**填空题未答态的输入控件**~~ **已确认**：按 `.answer-width` 逐空定位；题目容器居中后空格仍可能
   落在视口外，必须把每个空格单独 `scrollIntoView({block:'center'})`、重新读取坐标，再点击并用
   `Input.insertText` 输入。2026-08-27 实测 1/2/3/5 空题均成功输入英文逗号并提交为 `finished`。
3. **判断题未答态**两个 `.choice-btn` 的坐标/尺寸未采样（采样时已是答后状态）。
4. ~~未答题上卷尾提交按钮的可见性~~ **已确认**：未答态下卷尾 `.question-operation-area .btn-submit` 可见（120×40）。
5. `questionManyChance > 0`（多次作答机会）场景的 UI 差异：会出现题内 `submitQuestion` 按钮和"重做"（`redoQuestion`）。本次课程实测均为 0。
6. ~~contentType 5/6/7 映射~~ **已确认**：见 §2，7=交互页（可含视频+测验元素），非测验专属。
7. 视频插题弹出的测验与独立测验页在 DOM 上是否完全同构（推测同构，未对比）。
8. 卷尾 `submitQuiz` 点击瞬间的网络请求未抓包（不抓包、不碰后端接口，保持纯前端模拟路线）。
9. ~~未答完是否拦截~~ **已确认**：不拦截，弹 `.modal.fade.in` 确认框（见 §5.1），"确定离开"可放弃未答题继续翻页；章节末尾连弹两次。

## 13. `yxy_quiz.py` 已落实的修正

1. 已答判定使用 `finished` 类，不再误用 `show-answer`。
2. 判断题（type 4）点击 `.choice-btn`，默认占位选择“错误”。
3. 选择题按 `.option` 字母匹配，页面 DOM 顺序作为稳定后备信息。
4. 提交按钮固定取 `.question-operation-area > .btn-submit`（卷尾、`submitQuiz`）。
5. 每题操作前滚动并重读整页状态，避免解析区展开造成旧坐标失效。
6. 长页直接遍历 `.question-wrapper`；全部可答题完成输入/选择后，卷尾只提交一次。
7. 填空题按空格逐个滚动、重取坐标并输入配置占位符（默认英文逗号）。
