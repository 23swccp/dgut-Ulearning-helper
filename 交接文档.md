# 交接文档

## v0.3.5 浏览器识别补充包（2026-09-03）

- 文件选择标题和文件类型明确提示选择浏览器 `.exe`，程序内启动说明同步更新。
- 新增 QQ、搜狗、360 安全浏览器及 Opera GX 自动检测，补充旧版 360 极速、用户 Roaming 和常见目录别名。后端直接使用统一名单，避免扩充路径后被固定名单过滤；Edge、Chrome 仍优先。
- 扩大识别范围不代表完成这些浏览器的课程视频兼容验收，继续保留开头的兼容提示。
- 用户明确要求沿用 v0.3.5、保持原下载地址，并上传 GitHub；本次按该明确指示更新同标签下的安装器及配套更新资产，Gitee 由用户手动上传。GitHub main 推送带 [skip ci]，避免触发 Gitee 自动同步；不移动已发布标签，Release 说明指向本次源码提交。旧发布资产备份在忽略目录 `build/v035-github-before-replace/`。
- 同版本不会触发旧 v0.3.5 的自动更新，已有用户需重新下载安装；后续维护不能仅根据标签源码重跑构建来覆盖本次资产。完整包、从 v0.3.4 生成的增量包及清单必须配套上传。
- 验证：Python 173 项及 12 个子测试、前端 14 项及生产构建通过；恢复版本号后重新构建，v0.3.5 包完成两次 GUI/CLI 启动退出冒烟测试及安装器/清单版本校验。本地安装器位于 `dist/packages/v0.3.5-browser-update/`。

## v0.3.5 发布准备（2026-09-03）

本次发布包含浏览器分层扫描、原生文件选择、检测日志、程序内浏览器兼容提示，以及此前未发布的更新检查超时和更新界面修复。版本及手动补发默认标签已同步。GitHub 是更新源，Gitee 仅补传同一安装器；最终状态以双端发布验收为准。

## 原生浏览器文件选择（2026-09-03，纳入 v0.3.5）

- 检测失败后，先通过 `browser_dialog.py` 弹出 Windows 原生文件选择窗口，优先显示 Edge/Chrome 程序，可切换到其他 `.exe`。使用系统 comdlg32 的 Unicode 接口，不引入 GUI 框架或浏览器下载。
- 所选路径经过现有校验后保存；取消或窗口错误会回到终端，支持回车重新选择、粘贴路径/安装目录、输入 q 退出。浏览器配置完成后才启动网页，取消退出不打开默认浏览器。自动化 `--check` 模式发现浏览器缺失时返回非零退出码，不弹窗等待操作。
- 文件选择标题及终端提示推荐正式 Edge/Chrome，以降低课程视频兼容问题；其他 Chromium 仍可选择，当前未新增媒体编解码探测或保证任意浏览器都能播放。
- 本机原生窗口已实测选择 Edge 返回完整路径、取消返回空值；回归覆盖选择后再启动服务、取消重选/退出、弹窗异常时手动输入和无界面检查模式。随 v0.3.5 发布。
- 验证：完整 Python 回归 172 项及 5 个子测试通过；隔离数据目录的源码 `--check` 通过，结束后相关端口无监听残留。

## 浏览器分层扫描（2026-09-03，纳入 v0.3.5）

- 终端和网页检测共用 `_scan_browsers`：先检查 App Paths 注册信息、各本地固定磁盘及用户目录的常见安装路径（包含 `Application`、`App` 和浏览器根目录），再在浏览器自己的安装目录内广度优先查找已知程序名。
- 递归最多向下 3 层，每个安装目录最多检查 128 个目录、4096 个条目；不跟随目录符号链接或 Windows 联接，不递归遍历整个磁盘。自动扫描预算 10 秒，手动填写目录预算 5 秒；预算在文件操作之间检查，不能中断操作系统正在执行的单次文件/注册表访问。
- 手动填写浏览器顶层目录可找到其中嵌套的程序；填写完整 `.exe` 路径仍支持自定义程序名。未知品牌或不在这些安装位置的便携版仍可手动指定。
- 安装版检测过程保存到 `%LocalAppData%\DgutBot\data\browser-detection.log`，记录候选路径、扫描目录、找到的程序、权限错误、扫描上限及超时。终端检测失败时显示日志地址。
- 新增 `test_browser_paths.py` 并纳入发布工作流，覆盖 App 目录、中文嵌套目录、保存后重启、超时诊断、扫描边界和权限/联接处理。随 v0.3.5 发布；现有 v0.3.4 安装器不包含本次变更。
- 验证：完整 Python 回归 168 项及 5 个子测试通过；隔离数据目录的源码 `--check` 通过，结束后 1420–1439 / 8765–8784 无监听残留。本机只读检测约 0.06 秒找到 Edge 和 Chrome；朋友电脑仍需新安装包现场确认。

## 检查更新超时修复（2026-09-02，纳入 v0.3.5）

- Velopack 1.2.0 的同步检查没有显式网络超时；原适配层一直等待返回，导致界面停在“检查中”且不能再次点击。
- 检查现在最多等待 20 秒（包含 SDK 初始化和待安装包查询）；失败后恢复 idle 并显示具体错误。SDK 初始化独立于 UI 状态锁，状态轮询不会因此阻塞。
- 每次检查线程只交回结果，超时后的迟到结果不写状态、不触发自动下载。SDK 不能取消原生请求，未结束前禁止重试叠加请求；界面提示等待或重启，原请求结束后可重新检查。
- 设置页直接显示更新错误。更新源仍是 GitHub；修复随 v0.3.5 发布，需更新程序才能获得。
- 验证：`python -m pytest -q test_updater.py test_backend.py` 40 项及 3 个子测试通过，前端生产构建通过。独立只读 SDK 检查当前 GitHub 源约 6.5 秒返回无更新，未复现用户当时的网络阻塞。

## v0.3.4 发布准备（2026-09-02）

v0.3.3 在云端测试阶段因 Windows 临时目录短路径/长路径断言不一致而停止，未生成 Release 或安装器；保留已推送标签，修正断言后顺延至 v0.3.4。Gitee 同步工作流补充重新获取原始标签对象，避免 actions/checkout 将当前附注标签变成轻量标签后被 --follow-tags 忽略。

浏览器路径兼容、终端启动流程和 Agent CLI 一并发布。版本文件、关于页徽章及手动补发默认标签已同步。发布前完整 Python 回归 159 项及 5 个子测试、前端 14 项和生产构建通过。推送 main 后创建 v0.3.4 标签，使用既有 GitHub Actions 构建并同步 Gitee；最终发布状态以两端 Release 和工作流结果为准。

## 浏览器识别修复（2026-09-02，纳入 v0.3.4）

- `browser_paths.py` 统一处理网页设置、旧配置和终端输入的路径：清理复制产生的引号/方向标记，展开环境变量，支持浏览器 Application 文件夹。
- 自动识别保持 Edge → Chrome → 其他 Chromium 顺序，补查当前用户/整机、32/64 位 App Paths，以及系统盘标准路径和 ProgramW6432；不依赖默认浏览器关联来判断自动化浏览器。
- 设置保存前验证浏览器程序，无效路径通过 API 返回可读错误且不覆盖已有设置。旧配置中的带引号路径在读取时兼容。
- 安装版与源码版统一先显示启动终端，检测失败就在终端循环填写路径；验证通过才启动网页服务和浏览器。取消输入时直接结束，不再调用系统默认浏览器打开设置页。网页中的浏览器设置继续保留。
- 安装包保持 windowed 子系统，由交互入口通过 AllocConsole 创建终端并绑定输入输出；`--service` 后台和 Velopack 更新钩子不创建终端。朋友电脑的实际漏检原因尚未现场确认。
- 浏览器路径修复的后端、启动器、课程、测验与 Agent 工具回归 109 项及 3 个子测试通过。随后终端入口调整的后端、启动器、更新回归 64 项及 5 个子测试通过，覆盖安装版检测失败/取消输入不启动网页、路径重试后再启动、后台不创建终端，以及 UTF-8/GBK 终端编码。
- 源码与本地验证包 `--check` 均通过，`dist/browser-terminal-final/dgut-bot/` 完成两次完整 GUI/CLI 启动退出冒烟测试。隔离数据目录测试结束后 1420–1439 / 8765–8784 无监听残留；仅生成本地验证包，未改版本、未发布。

## Agent CLI控制平面（2026-09-02）

Agent CLI 与浏览器识别修复一同纳入 v0.3.4，`APP_VERSION` 已同步为 0.3.4。后续发布仍须按原流程增加新补丁版本，不能替换已有安装器。

### 模块与入口

| 文件 | 职责 |
| --- | --- |
| `agent_protocol.py`、`agent_schemas.py` | 版本 1 响应 DTO、稳定错误、Schema 校验和 ToolDefinition/ToolRegistry |
| `agent_tools.py`、`agent_service.py` | 19 个业务工具、唯一后端组合、课程任务关联与业务幂等 |
| `agent_tasks.py`、`agent_leases.py` | 任务 revision/终态、Condition 等待、幂等缓存与存活租约 |
| `agent_runtime.py` | 原子实例文件、当前用户权限、只读 PID 探测和实例发现 |
| `agent_cli.py` | `capabilities` / `call <tool> [--input <path>]`，无状态 UTF-8 JSON 客户端 |
| `quiz_requests.py` | QuizRequest 收件箱、答案校验、过期/取消/并发、AgentAnswerProvider |
| `yxy_quiz.py` | QuizReader、固定答案策略和 QuizExecutor；原 QuizHandler 兼容 GUI |
| `conftest.py` | pytest 收集前隔离数据目录，避免导入后端时读取真实凭据 |

依赖方向：GUI/CLI → HTTP → 注册表/业务服务 → 唯一 SignBackend/CourseController。MCP 暂不实现，未来适配器可复用注册表定义、dispatch、任务 DTO 和 EventBuffer，不需引入 SDK 到核心服务。

### 工具清单与使用流程

```text
system.health / system.version / system.capabilities
session.load_courses
course.list / course.select / course.start / course.get_status / course.set_speed / course.stop
task.get / task.wait / task.cancel
events.wait
quiz.list_pending / quiz.get_request / quiz.validate_answers / quiz.submit_answers / quiz.get_result
```

先启动 GUI/后台，再执行 `python agent_cli.py capabilities`。安装版使用 `%LocalAppData%/DgutBot/current/dgutctl.exe`（从 v0.3.4 开始提供）。CLI 不自动启动后台。`course.select` 只按精确 ID 更新选课，不打开网页；`course.start` 操作用户已经打开的课件页，仍从目录开头启动，不把选课动作误认为网页已切换。

`call` 空 stdin 等价 `{}`，支持 `--input` 替代 stdin。stdout 仅有一个 UTF-8 JSON 文档，退出码：0 成功、2 本地输入错误、3 发现/连接/认证错误、4 后台业务错误、5 协议不兼容/响应损坏、70 CLI 内部错误。非 JSON、非对象、非法 UTF-8 和超过 1 MiB 的输入会被拒绝。业务调用不接受 JS、坐标、任意 URL、shell 或文件操作。

所有写工具（包括 `session.load_courses`）要求 `idempotencyKey`。同工具同键同规范化输入返回首次结果；不同输入返回冲突。缓存实例内有效，容量 512、TTL 1 小时，未过期满容量时拒绝新写请求，绝不为腾空间提前遗忘副作用。任务最多保留 256 条，优先移除终态任务；等待使用 `afterRevision`/`afterSeq` 和 0–30000 ms 超时。事件只输出允许的语义字段，不转发原始调试日志。

`course.start` 立即返回 queued 任务，后台连接成功后转 running。Agent 默认 `quizMode=agent`，也可指定 `fixed` 或 `disabled`。GUI 未传新参数时维持现有配置。遇题时任务转 waiting_for_input，事件包含 QUIZ_PENDING/requestId；外部程序读取请求、校验答案、提交，再查询 `quiz.get_result`。提交接口返回 staged 接收结果，实际执行异步；同键重试仍返回首次接收结果，不重新点击。结果以 get_result 为准。

### 安全边界与已知限制

- Agent 路由不设置 CORS，旧 `/api/command` 继续兼容；未认证 `/api/health` 仅返回 `ok`。CLI 禁止认证请求重定向和代理转发。
- 施工说明要求 runtime 放 data_root，但现有开发模式 data_root 实际是源码目录。为满足“不在源码存令牌”优先边界，增加 `app_paths.agent_runtime_root()`：安装版与 data_root 相同；开发版运行时发现也固定到 LocalAppData。`YXY_DATA_DIR` 仍能隔离两者。其他开发配置位置不变。
- Windows 临时实例文件先限制为当前用户权限，再写令牌，最后原子替换；删除前核对 instanceId。PID 查询使用 OpenProcess/GetExitCodeProcess，禁止 Windows `os.kill(pid, 0)`。
- GUI 关闭不终止运行签到、课程、待答测验或 Agent 有界等待。原代码心跳实际为 **120 秒**，旧文档 90 秒是过时描述；主动关闭仍有 5 秒刷新宽限。
- 测验公开题型仅启用实测单选、判断、填空。多选/材料等明确 unsupported；绝不读取平台正确答案/解析。填空已有不同文本时失败并要求人工处理，不擅自追加或覆盖。
- 请求 revision 为当前语义快照版本（目前 1）；答案格式拒绝可在同 revision 重试，已过期/失败/取消/完成请求不能重放。全部格式校验后逐动作重新读取页面；输入焦点、选择结果/文本、卷尾提交和 finished 均需验证。
- Agent 等待答案不是媒体停滞，不触发看门狗恢复或导航。提交结果必须观察到同页题目 finished；页面提前消失时保守失败，不能当作成功或自动重交。合成场景通过不代表所有真实平台页面均已验收。
- 本次不公开签到控制、更新安装、应用关闭或敏感账号工具；未实现 MCP 或具体模型调用。

### 验证命令

```powershell
python -m pytest -q test_backend.py test_course.py test_launcher.py test_quiz.py test_updater.py test_gitee_release.py
python -m pytest -q test_agent_protocol.py test_agent_tools.py test_agent_cli.py test_agent_runtime.py test_agent_tasks.py test_quiz_requests.py
python browser_launcher.py --check
cd web
npm test -- --run
npm run build
cd ..
python scripts/pyinstaller_run.py packaging/dgut-bot.spec --noconfirm
python scripts/smoke_test.py dist/dgut-bot
```

冒烟测试应设置 `YXY_SMOKE_DATA_DIR` 为独立临时目录，且已有服务端口占用时拒绝开始。PyInstaller 两个 Analysis/EXE 经同一个 COLLECT 合并依赖，GUI 保持 console=False，CLI console=True。Windows 商店 Python 可用仓库包装脚本修复构建期解释器别名。正式发布仍走既有工作流，不能把本地测试安装器当成 Release。

本轮验收：完整 Python 回归 **147 项**、前端 **14 项**与生产构建通过；源码 `--check` 通过。发行包与升级后的安装目录均完成 GUI/CLI 冒烟测试。独立测试包 ID `DgutBotAgentCliValidation` 的 0.0.1 → 0.0.2 经官方 Update.exe 实际应用成功，CLI 文件哈希与对应构建匹配；测试快捷方式只指向 GUI。测试副本已通过官方卸载器移除，CLI、测试注册项和测试快捷方式均已清理，未替换正式 DgutBot 安装。

可用 `DGUTCTL_TEST_EXE` 指定构建的 `dgutctl.exe`，再次运行 `test_agent_cli.py`，把同一子进程协议测试应用于冻结入口。本地构建产物保留于 `dist/agent-cli-validation-20260902/dgut-bot/`；测试打包工具/安装器保留于忽略的 `build/agent-vpk-validation/`，仅供复核，不得上传为正式版本。真实课程作答未在本轮执行；测验验收使用合成 fixture。

## 自动更新架构（2026-08，Velopack）

| 文件 | 职责 |
| --- | --- |
| `version.py` | 运行时与发版的权威版本（`APP_VERSION`）；发版时同时同步前端包版本和关于页徽章 |
| `velopack_updater.py` | Velopack Python SDK 的薄适配层：检查、下载进度、待重启恢复、安装移交和前端消息；不自行替换文件 |
| `browser_launcher.py` | 主入口最先执行 `velopack.App().run()`；后台服务持有应用互斥锁，安装移交后停止任务、关闭助手标签页并退出 |
| `app_paths.py` | 将只读版本资源与持久数据分离；安装版数据固定在 `%LocalAppData%\DgutBot\data` |
| `yxy_mutex.py` | 仅保留 `Local\YxyAssistant.App` 应用互斥锁；下载和安装并发由 Velopack 管理 |
| `backend_commands.py` | 新增命令：`get_app_info`、`get_update_status`、`check_update`、`download_update`、`install_update`、`mark_update_read`、`ack_update_failure`；`shutdown_for_update` 由 `web_server.py` 特殊处理 |
| `web/src/updateClient.ts`、`safeMarkdown.tsx`、`UpdateDrawer.tsx`、`updateDrawer.css` | 前端：消息按钮 + 未读徽标、非模态消息抽屉（安全 Markdown、滚动位置恢复、底部固定状态区）、右上角进度 Toast、“暂不更新/立即更新”、一次性安装失败对话框 |
| `.github/workflows/release.yml` | tag（`v*`）推送后测试、构建 PyInstaller onedir，用官方 `vpk` 发布 GitHub Release，再上传用户安装器到 Gitee |
| `.github/workflows/sync-gitee.yml` | 将 GitHub `main` 和版本标签自动快进同步到 Gitee |
| `.github/workflows/gitee-release.yml`、`scripts/publish_gitee_release.py` | 手动补发已有版本；幂等创建/复用 Gitee Release 并替换同名安装器 |

移交时序：`install_update` → `wait_exit_then_apply_updates` 启动 Velopack `Update.exe` → 主程序进入 `waiting_for_exit` → 前端请求 `shutdown_for_update` → 停签到/刷课 → 经 CDP 只关闭匹配本机端口的助手标签页 → 服务进程退出并释放应用锁 → Velopack 替换 `current` → 从安装根目录的稳定启动器重启。若前端请求丢失，10 秒看门狗直接触发服务退出。

版本比较、包校验、增量或完整包选择、下载锁、安装与失败恢复均由 Velopack 负责。用户应通过安装器创建并由 Velopack 自动维护的快捷方式，或 `%LocalAppData%\DgutBot\dgut-bot.exe` 根目录启动桩进入；不能继续使用旧便携目录。测试见 `test_updater.py`。

## 项目状态

- 本地工作区名称可以不同，代码不要依赖开发者电脑上的绝对路径。
- GitHub 远程：`https://github.com/23swccp/dgut-bot.git`
- Gitee 镜像：`https://gitee.com/swccq23/dgut-bot.git`
- GitHub 是应用内更新源；Gitee 只提供源码镜像和完整安装器备用下载。
- 当前实现：Python 本地服务 + React/Vite 浏览器前端。
- 旧工作区 `C:\Users\34293\Desktop\优学院脚本` 已在合并验证后删除。
- 已移除：Tauri/Rust 桌面壳、sidecar、旧 Python GUI、旧发布包和重复启动入口。

## 目录职责

```text
dgut.yxy-checkin_assistant/
├─ 启动浏览器版.bat       Windows 唯一启动入口
├─ browser_launcher.py    浏览器检测、终端控制器、独立后台服务和退出生命周期
├─ web_server.py          仅监听回环地址的 HTTP API
├─ backend_commands.py    前端命令到后端能力的统一映射
├─ yxy_backend.py         登录、课程、签到监测、配置与日志
├─ yxy_course.py          课件页 CDP 学习辅助（视频/文档控制 + 测验事件接入）
├─ yxy_quiz.py            测验自动作答模块（QuizHandler，可独立运行）
├─ quiz_walk.py           实战辅助：章节内循环"翻页→清弹窗→答题"
├─ quiz_probe.py          临时探测工具：只读扫描测验页 DOM 结构
├─ run_brush.py           独立进程运行刷课控制器的调试驱动
├─ 测验页面结构调研.md    learnCourse 测验/目录/弹窗的真实 DOM 结构与机制记录
├─ web/                   React/Vite 前端
├─ test_backend.py        后端回归测试
├─ test_course.py         课件辅助回归测试
├─ test_quiz.py           测验作答回归测试（离线，不连浏览器）
├─ test_launcher.py       启动器与生命周期回归测试
└─ requirements.txt       Python 运行依赖
```

## 开发环境

- Windows 10/11
- Python 3.10+
- Node.js 18+
- Chromium 浏览器（优先 Edge/Chrome）

安装 Python 依赖：

```powershell
python -m pip install -r requirements.txt
```

前端依赖通常由启动器自动安装；手动安装和构建：

```powershell
cd web
npm ci
npm run build
```

## 验证命令

```powershell
python -m pytest -q test_backend.py test_course.py test_launcher.py test_quiz.py test_updater.py test_gitee_release.py
python browser_launcher.py --check
cd web
npm test -- --run
npm run build
```

`--check` 先验证浏览器配置，再短暂启动独立后台服务，检查本地 API、Vite 和代理后发出关闭命令；不会打开浏览器。

## 运行链路

1. `启动浏览器版.bat` 调用 `browser_launcher.py`。
2. 启动器设置终端编码：优先 UTF-8，失败时仅将终端输入输出回退到 GBK。
3. 启动器验证已保存浏览器路径；无效时按 Edge、Chrome、其他 Chromium 的固定顺序检查常见位置，失败后进入手动路径输入循环。
4. 浏览器确认后，启动器用实际安装目录中的 `pythonw.exe` 创建脱离终端的 `--service` 后台进程。
5. 后台进程自动选择空闲端口并创建 `ThreadingHTTPServer`；开发模式再启动 Vite，将 `/api` 代理到实际选中的 API 端口。发行版直接由本地服务托管 `web/dist`。
6. `web_server.py` 将 `/api/command` 交给 `backend_commands.py`，后者调用 `SignBackend` 或课件 `CourseController`。
7. 网页就绪后启动器以 Chromium 调试模式打开页面；此后关闭启动终端不会影响后台服务。
8. 浏览器前端每 2 秒发送心跳；关闭标签页后，后台进程清理 Vite 和 API，90 秒心跳超时作为兜底。

## 课件学习与测验作答架构

真实页面结构（选择器、题型代码、弹窗机制、坐标体系）全部记录在 `测验页面结构调研.md`，
改动测验相关逻辑前必读。核心分层：

- **yxy_course.py**：视频倍速/翻页、文档滚动、测验事件检测（注入脚本只观察不上报答案），
  `quiz-appeared/quiz-finished` 事件触发测验处理线程，测验期间挂起自动播放/滚动/防走神。
- **yxy_quiz.py 的 QuizHandler**：作答积木——按题型分发（单选/多选点选项字母、判断题点
  “正确/错误”按钮、填空题逐空输入英文逗号 `,`）、弹窗处理（前进/停留按钮文本匹配）、卷尾整套提交（`submitQuiz`
  一次判卷）、视口定位重试。通过构造参数注入 evaluate/click 回调，与连接方式解耦。
- **关键实测结论**：已答判据是 `.question-wrapper` 的 `finished` 类；提交按钮是卷尾
  `.question-operation-area > .btn-submit`（整套提交，点一次全部判卷）；未答完翻页/走神检测/
  章节统计共用 `.modal.fade.in` 壳，靠按钮文本区分方向。
- **新配置项**（`CourseConfig`）：`quiz_auto_answer / quiz_option_label / quiz_judgment_label /
  quiz_blank_text / anti_idle_scroll / anti_idle_interval`。防走神线程定期用真实 wheel 事件下滑再上滑，
  重置平台挂机计时，作答期间自动停手。

## 2026-08-27 真实课件最终验收

- CDP 实测课程：《管理统计学》（40 个目录页面）。最终根视图模型显示 `40/40` 页面记录完成，
  最后一页“女士品茶的故事”的填空题为 `finished`，课程终点由“本章统计/没有了”两种末页状态共同识别。
- 已真实覆盖：单页 2 个视频顺序播放、普通单视频、视频内测验、纯测验页、判断题、选择题、
  1/2/3/5 空填空题、富文本页、跨章统计页、未答完确认、最终章关闭弹窗和视频缓冲停滞。
- 填空题已确认可通过 `span.answer-width` 定位，逐空滚入视口后用 CDP `Input.insertText` 输入 `,`；
  真实题页均成功提交并变为 `finished`。
- 多视频完成不能只相信 `video.ended`：平台在结束后会重置播放器时间。实现使用
  `pageId + video index` 完成账本，并在同一 SPA 窗口内跨控制器重注入保留账本。
- `Runtime.enable` 会补发历史控制台消息。每次注入现在带唯一 session token，每条事件再带 pageId；
  Python 同时拒绝旧 session 和旧 page 的迟到事件，已实测阻止误翻页。
- 看门狗每 5 秒读取页面快照；连续 25 秒无进展会记录具体页面并恢复播放/题目/弹窗/导航。
  媒体持续缓冲时会暂停、轻微推进后重播，并自动将倍速减半（最低 2x）。CDP 断线会自动重连并重新注入。
- 后端命令 `get_course_helper_status` 可读取实时 `controllerState`、pageId/pageName、视频时间、
  未完成题数、弹窗、下一页和课程完成状态。因此“视频结束却未翻页”等停滞可被程序主动发现，无需盯浏览器。
- 最终回归：`python -m unittest -v test_backend.py test_course.py test_quiz.py test_launcher.py` 共 59 项通过
  （若后续新增测试，以当次输出为准）。

## 2026-08-28 刷课可观测化与后台运行验收

- 事件协议改为结构化游标流：后端统一生成 `seq/time/sessionId`，事件包含 `code/level/category/message/page/data`；
  前端保存 `lastSeq`，状态快照与事件流分开轮询。普通模式隐藏 CDP、注入、iframe 等调试信息。
- `CourseController.status_snapshot()` 提供真实运行态、CDP 连接、页码、页面任务计划、视频进度/倍速、
  最近推进时刻、重试次数与停滞原因。页面计划由视频、同源/跨域文档、测验及题型摘要组成，不再把一页压成单一类型。
- 目录 `recordComplete` 是最高优先级完成判据：已完成视频即使播放器被平台重置为 `00:00` 也保持暂停；
  视频结束而同页仍有文档/测验时不会翻页。翻页成功只接受 pageId、页码或可信内容变化。
- 启动会话先用目录中的 `#page{pageId}` 真实点击切回第一张可见页面，再用 pageId 验证；
  定位或验证失败即拒绝启动，避免用户停在最后一节时直接触发课程完成。
- `COURSE_COMPLETED` 是终止态：完成事件会停止看门狗、恢复与 CDP 控制；完成或手动停止写入会话时长，
  格式为 `04分06秒` 或 `01时02分03秒`。已修复末页完成后继续误报视频停滞的问题。
- 后台运行不再调用 `Page.bringToFront`。连接时启用 `Emulation.setFocusEmulationEnabled`，使 CDP 输入可在后台课件页生效。
  实测在未抢前台的情况下连续通过先前失败的第 6 页并推进至 20/40，未产生停滞或恢复事件。
- 当前课程回归：`python -m unittest -v test_course.py` 共 49 项通过；前端 `npm test` 与 `npm run build` 通过。

## 编码约定

- 终端编码是显示层兼容策略：UTF-8 可用时使用 UTF-8，旧控制台设置失败时回退 GBK。
- `config.json`、`auth.json`、`account.json`、签到日志、启动日志、后台日志和 HTTP JSON 均固定为 UTF-8。
- 不得因终端回退 GBK 而修改文件或接口编码；中文路径通过 Python Unicode/Windows 宽字符路径处理。

## 后续维护注意事项

- 新增前端命令时，同时修改 `backend_commands.py` 的白名单映射和相应测试。
- 不要把学校接口 Token、账号密码、浏览器 profile、日志或真实课程数据写进测试和提交。
- 修改启动流程后至少执行 `python browser_launcher.py --check`，并确认结束后没有占用 `1420–1439` 或 `8765–8784` 的残留进程。
- 修改终端输出时同时验证 UTF-8 主路径和 GBK 回退路径，避免把终端编码传播到文件或 HTTP 数据层。
- 修改课件注入脚本时必须保持“只观察、不点击”的边界：注入 JS 不得出现点击、提交、答案字样
  （`test_course.py` 的 `InjectScriptTests` 有静态断言）；所有页面动作一律经 Python 侧 CDP Input 域。
- 不要重新引入 `Page.bringToFront`：这会抢走用户当前标签页。后台课件需要输入焦点时使用
  `Emulation.setFocusEmulationEnabled`，并始终保留点击后的页面变化验证。
- 修改测验作答逻辑时同时运行 `test_quiz.py`；遇到新页面形态（新题型、新弹窗）先更新
  `测验页面结构调研.md` 再改代码，未知题型/按钮一律跳过并留日志，禁止盲点（测验限答 1 次）。
- 发布和更新流程已建立，不要另建平行发布方案或重新引入已删除的 Tauri 历史目录；所有维护遵循 `发布与更新维护.md`。

## Git 交接

合并工作完成在当前功能分支；提交前确认 `git status` 干净。普通提交推送 GitHub `main` 后会自动快进同步到 Gitee；只有明确发布新版本时才创建 `v*` 标签。发布步骤和双端验收见 `发布与更新维护.md`。个人运行数据由 `.gitignore` 排除，不随 Git 迁移。
