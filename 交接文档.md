# 交接文档

## 自动更新架构（2026-08 新增）

| 文件 | 职责 |
| --- | --- |
| `version.py` | 唯一版本来源（`APP_VERSION`），与 `web/package.json` 保持一致 |
| `yxy_updater.py` | 更新状态机 `UpdateManager`：检查 Release → 下载（.part 续传 + 3 次递增等待重试）→ SHA-256 校验 → 待安装；含 `compare_versions` / `parse_manifest` / `extract_zip_safely`（Zip Slip 防护）/ `select_targets_to_close`（CDP 精确关标签）；状态持久化 `.update/state.json`，刷新/重启可恢复 |
| `updater_installer.py` | 独立更新器（复制到临时目录运行，只依赖标准库）：取得 `Updating` 互斥锁 → ready 握手 → 等主程序退出 → 备份 → 解压替换（`config.json`/`auth.json`/`account.json`/`browser_profile/`/`签到记录.md`/日志永不覆盖）→ 校验 → 写 `update-result.json` → 重启；失败自动回滚，回滚失败进入手动恢复窗口 |
| `yxy_mutex.py` | Windows 命名互斥锁：`Local\YxyAssistant.App`（后台服务持有，防多开）、`Local\YxyAssistant.Updating`（更新器持有，期间拦截一切启动请求） |
| `backend_commands.py` | 新增命令：`get_app_info`、`get_update_status`、`check_update`、`download_update`、`install_update`、`mark_update_read`、`ack_update_failure`；`shutdown_for_update` 由 `web_server.py` 特殊处理 |
| `browser_launcher.py` | 启动时先检查 `Updating` 锁（弹窗拦截）再检查 `App` 锁；服务进程持有 `App` 锁；发布包模式（有 `web/dist` 且无 `node_modules`）由本地 API 服务直接托管前端，单端口运行 |
| `web/src/updateClient.ts`、`safeMarkdown.tsx`、`UpdateDrawer.tsx`、`updateDrawer.css` | 前端：消息按钮 + 未读徽标、非模态消息抽屉（安全 Markdown、滚动位置恢复、底部固定状态区）、右上角进度 Toast、“暂不更新/立即更新”、一次性安装失败对话框 |
| `.github/workflows/release.yml` + `scripts/package_release.py` | tag（`v*`）推送自动构建前端、打完整 ZIP（含 `web/dist`，不含 node_modules）并生成 `manifest.json`（version/url/size/sha256/changelog）上传 Release |

移交时序：`install_update` → 复制更新器到临时目录并启动 → 更新器写 ready 文件 → 主程序进入 `waiting_for_exit` → 前端 `window.close()` → 失败则后端经 CDP `Target.closeTarget` 只关闭匹配本机端口的助手标签页 → 停签到/刷课 → 服务退出释放 `App` 锁 → 更新器接管。

回滚结果记录在根目录 `update-result.json`；旧版本启动时弹一次对话框，确认后标记 `acknowledged`，之后只保留在消息抽屉。连续失败 3 次后停止自动安装（`update_failures.json`）。测试见 `test_updater.py`（30 项，含 Zip Slip、回滚、锁、握手、状态恢复等）。

## 项目状态

- 唯一保留目录：`C:\Users\34293\Desktop\dgut.yxy-checkin_assistant`
- GitHub 远程：`https://github.com/23swccp/dgut-Ulearning-helper.git`
- 当前实现：Python 本地服务 + React/Vite 浏览器前端。
- 旧工作区 `C:\Users\34293\Desktop\优学院脚本` 已在合并验证后删除。
- 已移除：Tauri/Rust 桌面壳、sidecar、旧 Python GUI、旧发布包和重复启动入口。

## 目录职责

```text
dgut.yxy-checkin_assistant/
├─ 启动浏览器版.bat       Windows 唯一启动入口
├─ browser_launcher.py    浏览器检测、终端控制器、独立后台服务和退出生命周期
├─ web_server.py          仅监听回环地址的 HTTP API
├─ backend_commands.py    前端命令到后端能力的统一映射
├─ yxy_backend.py         登录、课程、签到监测、配置与日志
├─ yxy_course.py          课件页 CDP 学习辅助（视频/文档控制 + 测验事件接入）
├─ yxy_quiz.py            测验自动作答模块（QuizHandler，可独立运行）
├─ quiz_walk.py           实战辅助：章节内循环"翻页→清弹窗→答题"
├─ quiz_probe.py          临时探测工具：只读扫描测验页 DOM 结构
├─ run_brush.py           独立进程运行刷课控制器的调试驱动
├─ 测验页面结构调研.md    learnCourse 测验/目录/弹窗的真实 DOM 结构与机制记录
├─ web/                   React/Vite 前端
├─ test_backend.py        后端回归测试
├─ test_course.py         课件辅助回归测试
├─ test_quiz.py           测验作答回归测试（离线，不连浏览器）
├─ test_launcher.py       启动器与生命周期回归测试
└─ requirements.txt       Python 运行依赖
```

## 开发环境

- Windows 10/11
- Python 3.10+
- Node.js 18+
- Chromium 浏览器（优先 Edge/Chrome）

安装 Python 依赖：

```powershell
python -m pip install -r requirements.txt
```

前端依赖通常由启动器自动安装；手动安装和构建：

```powershell
cd web
npm ci
npm run build
```

## 验证命令

```powershell
python -m unittest -v test_backend.py test_course.py test_quiz.py test_launcher.py
python browser_launcher.py --check
```

`--check` 先验证浏览器配置，再短暂启动独立后台服务，检查本地 API、Vite 和代理后发出关闭命令；不会打开浏览器。

## 运行链路

1. `启动浏览器版.bat` 调用 `browser_launcher.py`。
2. 启动器设置终端编码：优先 UTF-8，失败时仅将终端输入输出回退到 GBK。
3. 启动器验证已保存浏览器路径；无效时按 Edge、Chrome、其他 Chromium 的固定顺序检查常见位置，失败后进入手动路径输入循环。
4. 浏览器确认后，启动器用实际安装目录中的 `pythonw.exe` 创建脱离终端的 `--service` 后台进程。
5. 后台进程创建 `ThreadingHTTPServer` 并启动 Vite；Vite 将 `/api` 代理到 `127.0.0.1:8765`。
6. `web_server.py` 将 `/api/command` 交给 `backend_commands.py`，后者调用 `SignBackend` 或课件 `CourseController`。
7. 网页就绪后启动器以 Chromium 调试模式打开页面；此后关闭启动终端不会影响后台服务。
8. 浏览器前端每 2 秒发送心跳；关闭标签页后，后台进程清理 Vite 和 API，90 秒心跳超时作为兜底。

## 课件学习与测验作答架构

真实页面结构（选择器、题型代码、弹窗机制、坐标体系）全部记录在 `测验页面结构调研.md`，
改动测验相关逻辑前必读。核心分层：

- **yxy_course.py**：视频倍速/翻页、文档滚动、测验事件检测（注入脚本只观察不上报答案），
  `quiz-appeared/quiz-finished` 事件触发测验处理线程，测验期间挂起自动播放/滚动/防走神。
- **yxy_quiz.py 的 QuizHandler**：作答积木——按题型分发（单选/多选点选项字母、判断题点
  “正确/错误”按钮、填空题逐空输入英文逗号 `,`）、弹窗处理（前进/停留按钮文本匹配）、卷尾整套提交（`submitQuiz`
  一次判卷）、视口定位重试。通过构造参数注入 evaluate/click 回调，与连接方式解耦。
- **关键实测结论**：已答判据是 `.question-wrapper` 的 `finished` 类；提交按钮是卷尾
  `.question-operation-area > .btn-submit`（整套提交，点一次全部判卷）；未答完翻页/走神检测/
  章节统计共用 `.modal.fade.in` 壳，靠按钮文本区分方向。
- **新配置项**（`CourseConfig`）：`quiz_auto_answer / quiz_option_label / quiz_judgment_label /
  quiz_blank_text / anti_idle_scroll / anti_idle_interval`。防走神线程定期用真实 wheel 事件下滑再上滑，
  重置平台挂机计时，作答期间自动停手。

## 2026-08-27 真实课件最终验收

- CDP 实测课程：《管理统计学》（40 个目录页面）。最终根视图模型显示 `40/40` 页面记录完成，
  最后一页“女士品茶的故事”的填空题为 `finished`，课程终点由“本章统计/没有了”两种末页状态共同识别。
- 已真实覆盖：单页 2 个视频顺序播放、普通单视频、视频内测验、纯测验页、判断题、选择题、
  1/2/3/5 空填空题、富文本页、跨章统计页、未答完确认、最终章关闭弹窗和视频缓冲停滞。
- 填空题已确认可通过 `span.answer-width` 定位，逐空滚入视口后用 CDP `Input.insertText` 输入 `,`；
  真实题页均成功提交并变为 `finished`。
- 多视频完成不能只相信 `video.ended`：平台在结束后会重置播放器时间。实现使用
  `pageId + video index` 完成账本，并在同一 SPA 窗口内跨控制器重注入保留账本。
- `Runtime.enable` 会补发历史控制台消息。每次注入现在带唯一 session token，每条事件再带 pageId；
  Python 同时拒绝旧 session 和旧 page 的迟到事件，已实测阻止误翻页。
- 看门狗每 5 秒读取页面快照；连续 25 秒无进展会记录具体页面并恢复播放/题目/弹窗/导航。
  媒体持续缓冲时会暂停、轻微推进后重播，并自动将倍速减半（最低 2x）。CDP 断线会自动重连并重新注入。
- 后端命令 `get_course_helper_status` 可读取实时 `controllerState`、pageId/pageName、视频时间、
  未完成题数、弹窗、下一页和课程完成状态。因此“视频结束却未翻页”等停滞可被程序主动发现，无需盯浏览器。
- 最终回归：`python -m unittest -v test_backend.py test_course.py test_quiz.py test_launcher.py` 共 59 项通过
  （若后续新增测试，以当次输出为准）。

## 2026-08-28 刷课可观测化与后台运行验收

- 事件协议改为结构化游标流：后端统一生成 `seq/time/sessionId`，事件包含 `code/level/category/message/page/data`；
  前端保存 `lastSeq`，状态快照与事件流分开轮询。普通模式隐藏 CDP、注入、iframe 等调试信息。
- `CourseController.status_snapshot()` 提供真实运行态、CDP 连接、页码、页面任务计划、视频进度/倍速、
  最近推进时刻、重试次数与停滞原因。页面计划由视频、同源/跨域文档、测验及题型摘要组成，不再把一页压成单一类型。
- 目录 `recordComplete` 是最高优先级完成判据：已完成视频即使播放器被平台重置为 `00:00` 也保持暂停；
  视频结束而同页仍有文档/测验时不会翻页。翻页成功只接受 pageId、页码或可信内容变化。
- 启动会话先用目录中的 `#page{pageId}` 真实点击切回第一张可见页面，再用 pageId 验证；
  定位或验证失败即拒绝启动，避免用户停在最后一节时直接触发课程完成。
- `COURSE_COMPLETED` 是终止态：完成事件会停止看门狗、恢复与 CDP 控制；完成或手动停止写入会话时长，
  格式为 `04分06秒` 或 `01时02分03秒`。已修复末页完成后继续误报视频停滞的问题。
- 后台运行不再调用 `Page.bringToFront`。连接时启用 `Emulation.setFocusEmulationEnabled`，使 CDP 输入可在后台课件页生效。
  实测在未抢前台的情况下连续通过先前失败的第 6 页并推进至 20/40，未产生停滞或恢复事件。
- 当前课程回归：`python -m unittest -v test_course.py` 共 49 项通过；前端 `npm test` 与 `npm run build` 通过。

## 编码约定

- 终端编码是显示层兼容策略：UTF-8 可用时使用 UTF-8，旧控制台设置失败时回退 GBK。
- `config.json`、`auth.json`、`account.json`、签到日志、启动日志、后台日志和 HTTP JSON 均固定为 UTF-8。
- 不得因终端回退 GBK 而修改文件或接口编码；中文路径通过 Python Unicode/Windows 宽字符路径处理。

## 后续维护注意事项

- 新增前端命令时，同时修改 `backend_commands.py` 的白名单映射和相应测试。
- 不要把学校接口 Token、账号密码、浏览器 profile、日志或真实课程数据写进测试和提交。
- 修改启动流程后至少执行 `python browser_launcher.py --check`，并确认结束后没有占用 1420/8765 的残留进程。
- 修改终端输出时同时验证 UTF-8 主路径和 GBK 回退路径，避免把终端编码传播到文件或 HTTP 数据层。
- 修改课件注入脚本时必须保持“只观察、不点击”的边界：注入 JS 不得出现点击、提交、答案字样
  （`test_course.py` 的 `InjectScriptTests` 有静态断言）；所有页面动作一律经 Python 侧 CDP Input 域。
- 不要重新引入 `Page.bringToFront`：这会抢走用户当前标签页。后台课件需要输入焦点时使用
  `Emulation.setFocusEmulationEnabled`，并始终保留点击后的页面变化验证。
- 修改测验作答逻辑时同时运行 `test_quiz.py`；遇到新页面形态（新题型、新弹窗）先更新
  `测验页面结构调研.md` 再改代码，未知题型/按钮一律跳过并留日志，禁止盲点（测验限答 1 次）。
- 若未来发布安装包，应另建发布流程，不要重新引入当前已删除的 Tauri 历史目录。

## Git 交接

合并工作完成在当前功能分支；提交前确认 `git status` 干净。需要发布时，将该分支合并到主分支并推送远程。个人运行数据由 `.gitignore` 排除，不随 Git 迁移。
