# 功能列表

## 启动与生命周期

- 双击 `启动浏览器版.bat` 启动唯一的最新版。
- 自动检查 Python、Node.js/npm；首次运行缺少前端依赖时执行 `npm ci`。
- 启动 Python 本地 API（`127.0.0.1:8765`）和 React/Vite 前端（默认 `127.0.0.1:1420`）。
- 前端端口被占用时自动顺延选择空闲端口。
- 自动使用独立 `browser_profile/` 以 Chromium 远程调试模式打开前端。
- 关闭前端标签页 5 秒后自动结束本地服务；关闭信号丢失时由 90 秒心跳超时兜底。
- 网页命令框或启动器终端均支持 `kill`；网页命令框另支持 `clear`、`login`、`stop` 和 `/`。

## 浏览器与登录

- 自动检测 Microsoft Edge、Google Chrome、Brave 等 Chromium 浏览器，也可手动设置 `.exe` 路径。
- 在同一个调试浏览器中新增登录/课件标签页，不重复启动浏览器进程，不关闭用户原有浏览器窗口。
- 浏览器登录成功后读取 Cookie，保存本机 `auth.json` Token 缓存。
- 可选保存账号密码到本机 `account.json`；仅在 Token 缺失或课程请求返回 401 时尝试一次重新登录。
- 前端只显示是否已保存密码，后端不会把密码返回前端或写入日志。

## 课程与签到监测

- 从 Token 缓存或已登录浏览器读取课程列表。
- 在可视化课程页按课程 ID 精确选择课程。
- 持续轮询当日课堂活动，显示每轮检查、发现活动、处理结果和异常。
- 支持数字码、一键签到及已有二维码处理逻辑；不支持的活动会明确跳过并记录原因。
- 可开始、停止或取消当前课程监测。
- 可将签到结果追加写入 `签到记录.md`，并在设置中关闭日志或修改路径。

## 课件学习辅助

- 在用户已打开的 `ua.dgut.edu.cn/learnCourse` 课件页中通过 CDP 注入控制脚本。
- 支持视频倍速、播放恢复、章节提示框衔接和文档自动滚动。
- 可在前端保存倍速、提示框和文档滚动设置，并开始/停止学习辅助。
- 不获取答案、不填写或提交测验；测验由用户自行完成。

## 本地数据与安全边界

- 服务仅监听 `127.0.0.1`，不向局域网或公网提供接口。
- `config.json`：浏览器、日志和学习辅助设置。
- `auth.json`：Token 与用户 ID 缓存。
- `account.json`：用户明确启用时保存的账号密码。
- `browser_profile/`：独立调试浏览器配置和登录状态。
- 上述文件及日志均已加入 `.gitignore`。
