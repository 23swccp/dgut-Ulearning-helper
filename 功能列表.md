# 功能列表

## 启动与生命周期

- 双击 `启动浏览器版.bat` 启动唯一的最新版。
- 自动检查 Python、Node.js/npm；首次运行缺少前端依赖时执行 `npm ci`。
- 先确认并保存可用浏览器，再自动寻找空闲端口：发行版在 `8765–8784` 中使用单个静态服务端口；源码开发时，Python API 使用 `8765–8784`，React/Vite 使用 `1420–1439`。
- 前端端口被占用时自动顺延选择空闲端口。
- API 和 Vite 由独立无窗口后台进程持有；关闭启动终端不会中断网页、签到监测或刷课。
- 自动使用独立 `browser_profile/` 以 Chromium 远程调试模式打开前端。
- 关闭前端标签页 5 秒后由后台进程自动结束本地服务；关闭信号丢失时由 90 秒心跳超时兜底。
- 终端显示优先 UTF-8，设置失败时自动回退到 GBK；所有文件和网页数据仍固定使用 UTF-8。

## 浏览器与登录

- 有效的已保存路径优先；失效后按 Microsoft Edge、Google Chrome、Brave、360 等顺序检查 C/D 盘常见路径，检测最长 10 秒。
- 自动检测失败时在终端循环要求填写有效的 `msedge.exe` 或 `chrome.exe` 路径；支持英文双引号，格式或地址错误时不会继续启动网页。
- 在同一个调试浏览器中新增登录/课件标签页，不重复启动浏览器进程，不关闭用户原有浏览器窗口。
- 浏览器登录成功后读取 Cookie，保存本机 `auth.json` Token 缓存。
- 账号密码恢复能力仍为预留功能，前端学号和密码输入框暂时锁定，不对用户开放配置。
- Token 缓存失效时自动打开登录页；用户在程序浏览器中完成登录后，程序自动检测新缓存并继续读取课程。

## 课程与签到监测

- 进入签到模块后自动从 Token 缓存读取课程；缓存失效时自动打开登录页，登录完成后自动继续，不需要再按 Enter。
- 课程以终端内嵌图形选择器显示，支持课程名、教师和课程 ID 搜索，以及鼠标或上下方向键选择。
- 课程列表只在图形选择器显示，不重复打印文本清单；签到与刷课日志使用独立输出通道。
- 持续轮询当日课堂活动，显示每轮检查、发现活动、处理结果和异常。
- 支持数字码、一键签到及已有二维码处理逻辑；不支持的活动会明确跳过并记录原因。
- 可开始、停止或取消当前课程监测。
- 可将签到结果追加写入 `签到记录.md`，并在设置中关闭日志或修改路径。

## 课件学习辅助

- 在用户已打开的 `ua.dgut.edu.cn/learnCourse` 课件页中通过 CDP 注入控制脚本。
- 启动时先真实点击课程目录中的第一张可见页面，并用 pageId 验证已回到开头；无法验证时拒绝启动，避免当前停在末页就误报课程完成。
- 页面按可组合任务计划处理：视频、文档、测验可以同页共存；测验出现时优先处理，其余按视频、文档顺序执行。平台目录标记“已完成”优先于播放器停在 `00:00` 的外观，不会重播已完成视频。
- 支持视频倍速、播放恢复、章节提示框衔接和文档自动滚动；翻页仅在任务计划无待处理项时执行，并必须验证 pageId、页码或可信页面内容实际变化。
- 顶部状态栏独立展示运行状态、CDP 连接、当前课程/页面、页面流程、视频进度、倍速、最近推进时间、重试与停滞状态；关键事件与默认隐藏的调试日志分开显示。
- 后端事件使用带序号的游标队列，前端按 `afterSeq` 轮询，不因刷新或短暂重连重复或丢失关键事件。
- 课程完成后立即停止看门狗、恢复动作和 CDP 控制，状态显示“已完成”；完成或手动停止均记录后端会话用时，例如 `课程完成：40/40，用时 04分06秒`。
- 课件页可保持后台运行：不调用 `Page.bringToFront`。通过 CDP 焦点模拟让后台课件接收真实输入，仍对每次导航做页面变化验收。
- “刷课”模块使用独立的终端式操作区，可调整倍速并开始或停止课件学习辅助。

## 测验自动作答（实验性）

- 自动检测课件页中的未完成测验（整页练习和视频中途插题），触发独立作答线程，期间自动挂起视频播放与文档滚动。
- 选择题点击选项 C；判断题点击“错误”；填空题逐空填写英文逗号 `,`。
- 同一课件页有多个视频时按页面顺序逐个播放，全部结束后才翻页。
- 页面状态看门狗持续读取 pageId、视频时间、题目数、弹窗与下一页状态；25 秒无进展会自动恢复，持续缓冲时自动降速。
- 运行实例和页面事件均带唯一标识，CDP 重连或重启补发的旧控制台事件不会造成误翻页。
- 全部可选题目选完后统一点击卷尾提交（平台一次判卷），并验证每题完成状态。
- 选项定位失败自动重试（应对页面挂载期滚动被重置），重试耗尽只跳过该题，不中止整套；整套有失败时 30 秒后自动重试一轮。
- 自动处理三类弹窗：未答完确认（确定离开）、章节统计（继续下一章）、走神检测（继续学习）；按按钮文本匹配方向，未知按钮跳过并记录。
- 防走神保活：定期用真实滚轮事件下滑再上滑，重置平台挂机计时；测验作答期间自动暂停。
- 未知题型（如材料题）跳过并留日志；绝不盲目重试点击（测验限答 1 次）。
- 可在设置中关闭自动答题，或分别启用选择题、判断题与填空题的自动作答。
- 真实页面结构、题型代码与弹窗机制详见 `测验页面结构调研.md`。

## 本地数据与安全边界

- 服务仅监听 `127.0.0.1`，不向局域网或公网提供接口。
- `config.json`：浏览器、日志和学习辅助设置。
- `auth.json`：Token 与用户 ID 缓存。
- `account.json`：预留的账号恢复数据文件；当前前端尚未开放编辑。
- `browser_profile/`：独立调试浏览器配置和登录状态。
- `browser-launcher.log`、`browser-service.log`：UTF-8 启动与后台诊断日志。
- 上述文件及日志均已加入 `.gitignore`。

## 安装与更新

- GitHub Release 是正式发布源；应用通过 Velopack 从 GitHub 检查和下载更新。
- Gitee Release 提供相同完整安装器的国内备用下载，但不作为应用内更新源。
- 普通用户只需下载 `DgutBot-win-Setup-Qing-Zhi-Xia-Zai-Zhe-Ge.exe`；安装器包含完整程序，可在下载完成后离线安装。
- GitHub Release 的 `.nupkg`、`RELEASES` 和 `releases.win.json` 由自动更新使用，不是供普通用户手动安装的文件。
- 更新安装前，程序停止签到和刷课、关闭自己的浏览器标签页并退出；Velopack 完成原子替换后从稳定启动桩重新启动。
- GitHub 的 `main`、版本标签及用户安装器会由 Actions 自动同步到 Gitee。维护细节见 `发布与更新维护.md`。
