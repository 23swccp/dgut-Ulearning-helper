# 只读课件采样

`tools/quiz_probe.py` 是真实环境与本地课件实验室之间的结构采样工具。它附加到已打开的课件页，观察正常操作，不点击、不输入、不提交、不重放接口，也不启动刷课任务。输出不是题库，也不是可直接完整重放的平台副本。

## 如何采一份有效样本

1. 使用程序启动的调试浏览器，打开目标课件。采样时先停止自动课件任务，避免自动动作与手动步骤混在一起。
2. 在项目根目录运行以下命令。默认调试端口为 9222；如果程序用了其他端口，追加 `--port 实际端口`。

```powershell
python tools/quiz_probe.py --list
python tools/quiz_probe.py
```

默认仅匹配 URL 包含 `ua.dgut.edu.cn/learnCourse` 的标签；没有匹配就停止，不会改选其他网页。多个匹配时，先从 `--list` 获取目标 ID，再用 `--target-id "目标ID"` 精确指定。自定义 URL 关键字可作为第一个参数传入。

3. 初始快照显示框架数量后，在浏览器中正常操作；每个重要步骤后回终端输入下面的阶段名并回车。每次操作之间尽量稍作停留，避免几个状态挤在一次自动快照里。

| 阶段名 | 建议采集时机 |
| --- | --- |
| `before` | 题目已加载，尚未操作；限时测验的开始入口也单独采 |
| `selected` | 选中选项后 |
| `filled` | 填空输入后 |
| `submitted` | 正常提交后、等待结果时 |
| `result` | 成功、错误提示或成绩状态稳定后 |
| `next` | 切到下一课件后 |
| `error` | 遮挡弹窗、加载失败或其他异常出现时 |

直接回车也能打点；以上固定阶段名保留原样，其他自定义文字会被化名替换。输入 `q` 结束并生成最终报告，Ctrl+C 也会尽力保存已采内容。这里的“提交”始终由使用者在网页中正常完成；采样器只是观察。

仅保存一次静态结构，或不使用交互终端时：

```powershell
python tools/quiz_probe.py --once
python tools/quiz_probe.py --duration 180
```

自动观察默认：页面变化后的快照最小间隔 2 秒，即使无变化也每 15 秒保存一次；最多 60 份快照，每个框架最多 5000 节点，每次最多 64 个框架。较长流程可显式调整：

```powershell
python tools/quiz_probe.py --duration 300 --max-snapshots 180 --max-nodes 10000
```

达到容量、读取失败或事件丢失都会报告，不会把截断当作完整采集。主标签离开所选 URL 的协议/主机/路径时停止；仅查询参数变化允许继续。需要跨路径操作时为新页面另开采样。

## 样本包内容

默认保存到 `.course-captures/时间-随机编号/`，该目录已被 Git 忽略。

| 文件 | 用途 |
| --- | --- |
| `json/manifest.json` | 框架覆盖率、每份快照索引、接口结构、缺失/截断和停止原因；快照路径相对于此文件所在目录 |
| `json/NNN-snapshot.json` | DOM 层级、稳定节点编号、控件状态、位置、部分计算样式、ViewModel 状态、与前一快照的差异 |
| `NNN-frame_….html` | 不含原站脚本、资源请求和业务行为的静态脱敏结构预览 |
| `json/events.jsonl` | 点击/输入/焦点/滚动/DOM 变化及请求时序；不记录输入事件的文字 |

可覆盖同进程 iframe、跨域独立进程 iframe 及嵌套框架；显示上下级关系，并标识默认上下文或仅 DOM 的隔离上下文。能够识别到的开放 Shadow DOM 会采集，关闭的 Shadow DOM 无法读取。

网络仅观察采样开始后的 Document、XHR、Fetch；不导出请求头或响应头。JSON 和 URL 编码表单保留字段及类型，不保留正文值；表单中的 JSON 也转换为结构。JSON 回执仅额外保留有限的成功布尔值/小范围状态码。非 JSON 回执、超大正文及未能取到的正文明确标记省略/不可用。已发生的请求不会补发，因此要采提交协议，必须先启动采样再正常提交。

## 如何判断够不够

不要只看退出成功。先看 `json/manifest.json`：

- `quality.allFramesCaptured` 应为 `true`，逐份 `coverage.complete` 应为 `true`。
- `issues` 应为空，`cdpEventsDropped` 与 `networkOmitted` 应为 0。
- 需要的提交请求、成功/失败回执应该实际出现在 `network` 中；`networkBodiesUnavailable` 非零需核查。没有提交记录可能只是此次没有执行提交，不代表接口已验证。
- `before → selected/filled → submitted → result` 应能找到状态差异。只有一张静态页面不足以推断提交规则。

退出码：0 表示本次结构采样没有发现已知覆盖问题；2 表示已保存但有覆盖/截断等问题；1 表示采样错误；130 表示人为中断。退出码 0 **不是**“接口齐全”或“真实刷课已验证通过”。

少量真实样本应按结构类别选：普通/限时测验、不同填空控件、图片/公式、视频、同源/跨域文档/PPT、阻塞弹窗各选代表。重复采同一种普通题不能弥补其他类别缺失。

拿到样本后仍需把实际 DOM 差异、状态转换与接口规则转化为本地合成 fixture，增加对应断言，再跑 `tools/course_lab.py`。静态 HTML 不能自动模拟平台的鉴权、计时、保存判定和业务错误；AI 解答本身的正确率也不由这套传输/浏览器测试保证。

## 脱敏与边界

默认用单次采样内稳定的化名替换题干、输入值、页面 ID 等，移除 URL 参数值与片段；保留结构类名、绑定字段、域名和必要的控件标签，便于诊断。化名密钥不落盘，不同采样包不能用化名直接关联。脱敏不是对任意自定义网页的绝对匿名保证，分享前仍应检查包中的结构字段和站点域名。

默认不截图。如确有图片、公式或布局问题，可主动追加 `--private-screenshots`：原始视口截图保存在 `private/`，**未脱敏**，可能包含账户、题目和答案；仅本地检查，分享包时排除该目录。不开此开关也会保留图片/画布的类型与几何信息，但不会采到其中的文字内容。不要把任何真实样本提交进仓库。

浏览器需支持本机调试连接；本工具不读取登录缓存、Cookie、磁盘浏览器资料或登录凭据。运行结束移除自己的监听器并断开连接，不关闭使用者的浏览器。采样存在布局读取、事件观察和 CDP 带来的开销，不应把采样中的绝对耗时视作无干扰性能基准。

## 工具自身的验收

```powershell
python -m pytest -q tests/test_course_capture.py
python tools/capture_smoke.py --browser "C:\Program Files\Google\Chrome\Application\chrome.exe"
python tools/capture_smoke.py --browser "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe"
```

合成站点独立验证四个同源/跨域/嵌套框架、开放 Shadow DOM、真实 Input 事件、状态差异、请求与回执结构、刷新恢复、监听器移除、CLI 和显式截断。预埋敏感哨兵检查所有导出文本没有原文，并确认单独采样不会触发点击、输入和提交。它只使用临时浏览器资料及合成数据，不连接真实账号；结果保存在 `.course-lab/capture-浏览器名.json`。对应检查已加入浏览器矩阵工作流，尚需推送后才会在云端执行。
